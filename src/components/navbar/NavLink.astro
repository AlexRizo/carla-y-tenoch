---
import { cn } from "@/utils/cn";
import type { AstroComponent } from "@lucide/astro";

interface Props {
  href: string;
  label: string | AstroComponent;
  labelKey?: string;
  className?: string;
}

const { href, label, labelKey, className } = Astro.props;

const Icon = label as AstroComponent;
---

<a href={href} class={cn("text-lg text-white", className)} data-i18n={labelKey}>
  {typeof Icon === "string" ? label : <Icon />}
</a>
