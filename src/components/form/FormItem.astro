---
interface Props {
  id: string;
  label?: string;
}

const { id, label } = Astro.props;
---

<div class="flex flex-col gap-1 lg:min-w-78">
  {label && <label class="text-xs" for={id}>{label}</label>}
  <slot />
</div>
