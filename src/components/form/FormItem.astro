---
interface Props {
  id: string;
  label?: string;
  labelKey?: string;
}

const { id, label, labelKey } = Astro.props;
---

<div class="flex flex-col gap-1 lg:min-w-78">
  {label && <label class="text-xs" for={id} data-i18n={labelKey}>{label}</label>}
  <slot />
</div>
